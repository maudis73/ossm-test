apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: east-ingress-route
  namespace: east-ingress
spec:
  parentRefs:
    - name: east-ingress
      namespace: east-ingress
  hostnames:
    - east-ingress.example.com
  rules:
    # One path per rule: multiple matches in the same rule are ANDed (all must match).
    - matches:
        - path:
            type: PathPrefix
            value: /status
      backendRefs:
        - name: httpbin
          port: 8000
    - matches:
        - path:
            type: PathPrefix
            value: /headers
      backendRefs:
        - name: httpbin
          port: 8000
    - matches:
        - path:
            type: PathPrefix
            value: /hello
      backendRefs:
        - group: ""
          kind: Service
          name: helloworld
          namespace: sample
          port: 5000
