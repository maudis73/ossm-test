# Allows HTTPRoute in west-ingress to use Service helloworld in this namespace.
apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: west-ingress-to-helloworld
  namespace: sample
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      namespace: west-ingress
  to:
    - group: ""
      kind: Service
      name: helloworld
